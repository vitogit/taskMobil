<%= form_for(@task,:remote => true)  do |f| %>
    <% if @task.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

          <ul>
            <% @task.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.text_field :name, :placeholder => "Name task" %>

    <%= f.text_field :status, :placeholder => "Status task" %>
    <%= f.button "Post", :remote=>true, :type => "submit", :class => "btn btn-default" %>
<% end %>